```
═══════════════════════════════════════════════════════════════════
NEXUS — MULTI-AGENT CROSS-VERIFICATION INSTRUMENT
TEMPLATE — v0.4  (blank-to-fill; instruction-complete)
═══════════════════════════════════════════════════════════════════
Classification : DRAFT / PROPOSED / ADVISORY — not promoted
Authority      : Sovereign Operator (Mase) — sole root
Assembler      : C1 (Claude web chat) — consolidates; does NOT author others' slots
Protocol basis : Co-Dev Protocol v1.0 — ACTIVE (operator ruling 2026-05-29, OD-1 resolved)
Slots          : C1 C2 C3 C4 C5 C6 C7  (fill your OWN only)
Use            : each member fills its slot independently, THEN C1 assembles,
                 THEN operator signs off (Part 6). Re-run per verification cycle.
═══════════════════════════════════════════════════════════════════

⚠ SLOT INTEGRITY INSTRUCTION  [binding]
• Fill ONLY your own slot, from your OWN environment.
• Do NOT edit another member's slot, restructure this document, or author a
  competing framework.
• Spotted a problem in another slot? Raise it as a Part-5 MATRIX NOTE —
  never by editing that slot. (Origin: an agent edited C1 = S-4 breach.)
• Framework/standard changes are PROPOSED to the operator separately —
  never enacted by editing this file.
• FAULT LOGGING (ADR-007): if a protocol deviation or operator override
  occurs, log it in 04_logs/audit/fault_log.md with an ADR-007 reference.
  Do NOT self-adjudicate recall conflicts — log them as discrepancies and
  resolve in the workspace's favor (ADR-004 / S-2).

⚠ ASSEMBLER PROTOCOL
• C1 (web chat) is the designated assembler: collects filled slots, lays them
  out, completes the Part-5 matrix, versions the doc. Assembly = transcription
  + structuring ONLY; the assembler may not write another member's content.
• Each consolidated build increments the version and lists its changes.

───────────────────────────────────────────────────────────────────
PART 1 — PROPOSED DRAFT STANDARD
NEXUS Multi-Agent Cross-Verification & HUD Agent Conduct
Status: DRAFT — PENDING OPERATOR PROMOTION
───────────────────────────────────────────────────────────────────
S-1  Every agent is ADVISORY. No execute/write/promote without an explicit
     operator gate.
S-2  Source hierarchy (binding): operator correction > doctrine registry >
     active standards > locked support records > live workspace files >
     generated outputs > memory recall > interface surfaces.
S-3  Boundary axioms hold at every layer:
     governance≠memory · memory≠runtime · runtime≠interface
     retrieval≠validation · aggregation≠truth · recall≠approval
S-4  CROSS-VERIFICATION INTEGRITY: each member fills its own slot from its own
     environment. No fabricating another's report. Inference about another
     member = [INFERENCE], never promoted to [EVIDENCE].
S-5  Provenance labels REQUIRED on claims: REFERENCE / EVIDENCE / RECALL /
     INFERENCE / ADVISORY / FLAG / OBSERVATION / VERIFICATION / DISCREPANCY.
S-6  Confidence REQUIRED on non-trivial claims: high / medium / low.
S-7  Memory (incl. Pieces LTM) is recall-only (ADR-004). Conflict with
     workspace/operator = DISCREPANCY SIGNAL, not authority.
S-8  Drift control: each member states projects/ideals developed in ITS OWN
     application — no borrowing credit from another surface.
S-9  Tier-1 actions (writes/edits/irreversible) follow the full Co-Dev
     ceremony: proposal → sandbox → mode switch → Y → execute → halt → revert.
     Faults during any action are logged per ADR-007 (Operator Fault Logging).
S-10 This instrument is a foundational EXAMPLE, not promoted doctrine, until
     operator sign-off (Part 6).

───────────────────────────────────────────────────────────────────
PART 2 — FOUNDATION CORE   [verified baseline 2026-05-29 — RE-VERIFY each cycle]
C6 (live disk) is authoritative over recall (S-2). Confirm before relying.
───────────────────────────────────────────────────────────────────
• Root: C:\Users\mason\Documents\PROJECTS\VS_CODE_NEXUS  (NEXUS Base V1, ACTIVE).
• Lanes on disk: 00→08 (+docs). 08_security_index EXISTS. 05_experiments empty;
  09_ empty (note: tracked 09_presentation/ also exists from origin).
• Kernel: v1.2 (KERNEL_VERSION.md, Stable, 2026-03-18). ✓
• ADRs: 001–004 present; 005/006 absent (numbering artifact); 007 AUTHORED
  2026-05-29 as Proposed (docs/adr/ADR-007-operator-fault-logging.md).
• Active standards: 11 on disk (manifests still say 8 — stale, FL-002).
• Doctrine: 12 canonical (registry + both manifests + 12 files on disk). ✓ (C4/C6)
• Co-Dev Protocol v1.0: ACTIVE (operator ruling 2026-05-29, OD-1 resolved).
• Tri-sync (3-plane): VS Code ↔ Claude ↔ Pieces LTM (ADR-004). ≠ 4-tool dev
  orchestration. HUD ≠ Hub. Do not conflate.
• HUD: Stage-1 scaffold, PRE_DRAFT, AT CHECKPOINT 1.10. No build dir; no
  authorization past 1.10 on disk.
• Discrepancies: DR-001 terminology · DR-002 AEGIS lifecycle · DR-003 Council
  arch · DR-004 KAIROS/KAIROZ. Plus a DR-namespace collision (Part 3 P-8).

───────────────────────────────────────────────────────────────────
PART 3 — PENDING-PROMOTION REGISTER   [nothing here is promoted]
───────────────────────────────────────────────────────────────────
P-1  NEXUS HUD as "next baseline" — proposed, not promoted. Operator decision.
P-2  This Cross-Verification Standard (Part 1) — DRAFT.
P-3  Co-Dev Verification Protocol V1 — supersedes Banana V0 (in design).
P-4  ADR-007 (Operator Fault Logging) — AUTHORED 2026-05-29 as Proposed.
     Awaiting operator acceptance to flip §9 from interim to full pointer.
P-5  Connector-surface posture — trim to working set; connect-on-demand; Drive call.
P-6  Tool-access = on demand + connector trim. Settled-pending.
P-7  nexus-deep-dive-reporter skill — install/run authorization pending.
P-8  DR-namespace collision — governance DR-001..004 vs a second DR-001..008 in
     NEXUS_SUXEN_MASTER_REPORT_v1.md. Needs a canonical-DR-register decision.
P-9  Framework-stacking — adopt ChatGPT's Provenance Review Procedure as a
     claim-level method UNDER this member-level instrument? HELD (operator Y/N).

───────────────────────────────────────────────────────────────────
PART 4 — PER-MEMBER ASSESSMENT SLOTS
Each block states the member's ROLE, what to DO, and what to PROVIDE.
Fill in [EVIDENCE]/[RECALL]/[INFERENCE] with confidence (S-5/S-6). Your OWN slot only.
───────────────────────────────────────────────────────────────────

┌─ C1 — CLAUDE (web chat) ── ROLE: Architect & Orchestrator (advisory) + Assembler ┐
│ DO: give advisory architecture/orchestration; assemble this instrument from   │
│   members' filled slots (transcribe + structure, never author their content); │
│   flag your own drift.                                                         │
│ PROVIDE:                                                                       │
│  • Identity & role this session.                                               │
│  • Contributions [EVIDENCE] — what you actually produced in-thread.            │
│  • Boundaries held [EVIDENCE] — corrections made, claims you refused to        │
│    promote, terminology discipline.                                            │
│  • Limits [INFERENCE/known] — no live access to other runtimes; recall≠truth.  │
│  • PROVENANCE (S-8): ideals/projects advanced on the web-chat surface only.    │
│  • Confidence: high/med/low and on what.                                       │
│ INTEGRITY: advisory only; cannot author other slots; assembler ≠ author.       │
└────────────────────────────────────────────────────────────────────────────────┘

┌─ C2 — CLAUDE CODE (VS Code extension) ── ROLE: Primary Implementation, local plane ┐
│ NOTE: distinct from the C6 workspace inspection — do NOT repeat C6 here; this  │
│   is your AGENT-CONDUCT self-report.                                           │
│ DO: run read-only inspections; execute Tier-1 writes ONLY after an operator    │
│   gate (Co-Dev ceremony); keep a Tier-1 ledger; log faults per ADR-007;        │
│   never push except at a milestone (local-first).                             │
│ PROVIDE:                                                                       │
│  • Identity/role as the local execution plane (FS + git + shell).              │
│  • Tier-1 ledger of YOUR actions — for each: read-only or write? gated?        │
│    git HEAD/branch/commit hashes touched.                                      │
│  • Self-reported vs file-confirmed — separate [EVIDENCE] from [INFERENCE].     │
│  • STRATEGIC: value local execution adds that web chat (C1) structurally can't.│
│  • STRATEGIC: contradictions vs ground truth beyond what C6 logged.            │
│  • PROVENANCE (S-8): what YOU advanced in VS Code.                             │
│  • Confidence: high/med/low and on what.                                       │
│ INTEGRITY: advisory + GATED executor; not governance authority; every write    │
│   operator-gated; every inspection read-only.                                  │
└────────────────────────────────────────────────────────────────────────────────┘

┌─ C3 — CHATGPT / NEXUS_SUXEN (incl. Pulse) ── ROLE: Verifier / auditor (no local API) ┐
│ DO: synthesize/audit from LOADED sources; enumerate SUXEN chats + Pulse        │
│   artifacts with disposition; surface divergences (divergence is the value).   │
│ PROVIDE:                                                                       │
│  • Identity + which sources/lineage are loaded (mirrored/read-only ≠ live).     │
│  • Base-V1 evidence visible in loaded sources [EVIDENCE] + its boundary.        │
│  • Active SUXEN chats [RECALL] and Pulse artifacts + status/disposition.        │
│  • Verifier-vs-source boundary — what you CAN vs CANNOT verify.                 │
│  • Divergence signals [INFERENCE] — where you diverge from Claude/operator.     │
│  • Terminology/glossary risks. • Limits. • PROVENANCE (S-8). • Confidence.      │
│ INTEGRITY: cannot verify live FS or other runtimes; label anything not in       │
│   loaded context as [INFERENCE]; no fabricating other members' reports.         │
└────────────────────────────────────────────────────────────────────────────────┘

┌─ C4 — CODEX (GPT in VS Code) ── ROLE: Second opinion / structured extraction ──────┐
│ DO: run repo analytics / structured extraction; write NO implementation before │
│   a signed contract (HUD README); surface inventory gaps others can't see.     │
│ PROVIDE:                                                                       │
│  • What analytics/extraction you ran [EVIDENCE].                                │
│  • Any implementation written + against which signed contract.                  │
│  • Inventory gaps / structural findings.                                        │
│  • Disk-confirm counts when assigned (e.g., doctrine/artifact counts).          │
│  • PROVENANCE (S-8). • Confidence.                                              │
│ INTEGRITY: no writes before contract sign-off.                                  │
└────────────────────────────────────────────────────────────────────────────────┘

┌─ C5 — PIECES OS LTM (on VS Code) ── ROLE: Memory layer, recall-only (Plane 3, ADR-004) ┐
│ DO: report what LTM actually captured this period; flag any recall that        │
│   conflicts with the workspace as a DISCREPANCY SIGNAL — do not resolve it in   │
│   Pieces' favor.                                                                │
│ PROVIDE:                                                                       │
│  • Conversations/snippets captured this period [OBSERVATION].                   │
│  • Any recall conflicting with workspace (log it; don't adjudicate).            │
│  • Where continuity recall helps handoffs without becoming authority.           │
│  • PROVENANCE (S-8): N/A as author — Pieces witnesses, doesn't develop.          │
│  • Confidence.                                                                  │
│ INTEGRITY: recall-only, advisory; output = ADVISORY RECALL ONLY; never authority.│
└────────────────────────────────────────────────────────────────────────────────┘

┌─ C6 — NEXUS PROJECT FOLDER (workspace) ── ROLE: SYSTEM, assessed by inspection ────┐
│ EXECUTED BY: Claude Code (local plane), READ-ONLY. A system, not an agent.     │
│ DO: inspect live disk; make ZERO mutations; label every line EVIDENCE or FLAG; │
│   cite file:line; resolve recall-vs-disk conflicts in disk's favor (S-2).      │
│ PROVIDE:                                                                       │
│  • Git: HEAD, branch, ahead/behind, clean?, last commits, notable untracked.    │
│  • Lanes present; active-standards count; ADR set; kernel version.              │
│  • HUD stage (build dir? authorization past checkpoint?).                       │
│  • DR register state + any new drift.                                           │
│  • Tier-1 ledger: must confirm ZERO mutations.                                  │
│  • PROVENANCE: workspace is the substrate, authoritative over memory.           │
│ INTEGRITY: inspector must not mutate; disk beats recall.                         │
└────────────────────────────────────────────────────────────────────────────────┘

┌─ C7 — MANUS (Autonomous Cloud VM Agent) ── ROLE: Autonomous agent, isolated VM ────┐
│ DO: parse/validate the instrument; enforce your own cloud-VM isolation; advise  │
│   only, zero unauthorized execution. Spotted an issue in another slot? Raise a  │
│   Part-5 MATRIX NOTE — never edit that slot.                                     │
│ PROVIDE:                                                                       │
│  • Identity + environment (isolated VM).                                        │
│  • Contributions [EVIDENCE] this period.                                        │
│  • Boundaries held (operator = sole root; advisory; refused C6 — no local FS).  │
│  • Limits [INFERENCE/known]: no local VS_CODE_NEXUS access; no shared memory.    │
│  • PROVENANCE (S-8). • Confidence.                                              │
│ INTEGRITY: hard isolation; cannot perform C6; must not edit other members' slots.│
└────────────────────────────────────────────────────────────────────────────────┘

───────────────────────────────────────────────────────────────────
PART 5 — CROSS-REFERENCE MATRIX   [assembler completes AFTER all slots filled]
Conflicts are the deliverable. Resolve per S-2 (workspace wins), not by vote.
───────────────────────────────────────────────────────────────────
            | C1 | C2 | C3 | C4 | C5 | C6 | C7 |
   C1 Claude| ░░ |    |    |    |    |    |    |
   C2 C.Code|    | ░░ |    |    |    |    |    |
   C3 GPT   |    |    | ░░ |    |    |    |    |
   C4 Codex |    |    |    | ░░ |    |    |    |
   C5 Pieces|    |    |    |    | ░░ |    |    |
   C6 Folder|    |    |    |    |    | ░░ |    |
   C7 Manus |    |    |    |    |    |    | ░░ |
   Legend: ✓ agree · ⚠R conflict→resolved by C6 · ⚠B integrity breach ·
           ⚠U unverified · ~ partial · → covers/supersedes
   • List RESOLVED CONFLICTS (with the winning source class).
   • List genuine AGREEMENTS (independent cross-confirmation).
   • List OPEN / NEW items. • Note which slots are still PENDING.

───────────────────────────────────────────────────────────────────
PART 6 — OPERATOR SIGN-OFF / PROMOTION GATE   [operator only]
───────────────────────────────────────────────────────────────────
[ ] Foundation Core (Part 2) verified against live workspace this cycle
[ ] All seven slots completed independently (___/7)
[ ] Cross-reference matrix resolved
[ ] Open decisions cleared (list): __________________________________
[ ] Accept ADR-007 (Operator Fault Logging)?   Y / N
[ ] Promote Part-1 Standard?                    Y / N
[ ] Name HUD as formal next baseline?           Y / N
Operator signature / marker: __________   Date: __________

───────────────────────────────────────────────────────────────────
APPENDIX — OPERATOR-DECISION CANDIDATES  [surfaced, NOT executed]
───────────────────────────────────────────────────────────────────
1. Duplicate DEF-13 commits (44c57a2 ≡ 6a647ba): history cleanup or leave?
2. main behind origin/main by 1 AND local-work ahead (unpushed): reconcile/push?
3. DR-namespace collision (P-8): pick the canonical DR register.
4. Empty lane 05_experiments: confirm intended or prune.
5. HUD: confirm remains at 1.10; correct any seed data saying "authorized."
6. Manifest regeneration (FL-002): regenerate both KERNEL_MANIFEST.json against
   00_governance_ref/ (8→11 standards; drop phantom META_CONTROL_CENTER.md).
7. Accept ADR-007 (flips Co-Dev §9 from interim discipline to full pointer).
8. Framework-stacking (P-9): adopt ChatGPT Provenance Procedure under this instrument?
═══════════════════════════════════════════════════════════════════
END — TEMPLATE v0.4 — DRAFT, operator-gated
═══════════════════════════════════════════════════════════════════
```

---

**Reminder text for the assembler's canonical instrument** (add to its Slot Integrity Instruction):

> • FAULT LOGGING (ADR-007): if a protocol deviation or operator override occurs, log it in `04_logs/audit/fault_log.md` with an ADR-007 reference. Do NOT self-adjudicate recall conflicts — log them as discrepancies and resolve in the workspace's favor (ADR-004 / S-2).

*Note: this file is a local template artifact (Claude Code / C2). The canonical, version-bumped instrument is maintained by the assembler (C1); this is provided for adoption, not as a competing copy.*
